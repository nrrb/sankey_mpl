{
  "name": "sankey-mpl-golden-tools",
  "private": true,
  "type": "module",
  "description": "Regenerates the dataset fixtures and their golden geometry by running the reference JavaScript implementation. Never installed: the wheel ships only src/sankey_mpl. Travels in the sdist so a source release can rebuild its own goldens.",
  "scripts": {
    "generate": "node generate_golden.mjs"
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.5",
    "chart.js": "^4.5.1",
    "chartjs-chart-sankey": "^0.15.0"
  }
}
